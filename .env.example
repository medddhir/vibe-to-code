# Supabase project settings. Use a separate project for each environment.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Enable only after the matching database migration is applied and verified.
NEXT_PUBLIC_PROGRESS_SYNC_ENABLED=false
